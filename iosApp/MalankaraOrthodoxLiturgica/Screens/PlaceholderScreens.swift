// Temporary stand-in, replaced by its real implementation in a later task:
// QrScannerPushedView (Task 14).
import SwiftUI

struct QrScannerPushedView: View { var body: some View { Text("QR Scanner") } }
